UPDATE clientes SET `nome` = UPPER( `nome` );
UPDATE clientes SET `email` = LOWER( `email` );
UPDATE clientes SET `descricao` = LOWER( `descricao` );
UPDATE clientes SET `rua` = UPPER( `rua` );
UPDATE clientes SET `complemento` = UPPER( `complemento` );
UPDATE clientes SET `bairro` = UPPER( `bairro` );
UPDATE clientes SET `cidade` = UPPER( `cidade` );
UPDATE clientes SET `estado` = UPPER( `estado` );
UPDATE clientes SET `profissao` = UPPER( `profissao` );
UPDATE clientes SET `naturalidade` = UPPER( `naturalidade` );
UPDATE clientes SET `responsavel` = UPPER( `responsavel` );
UPDATE clientes SET `mes` = UPPER( `mes` );
UPDATE clientes SET `sexo` = UPPER( `sexo` );